FROM vllm/vllm-openai:v0.8.3

# Le modèle sera monté via volume à l'exécution.
# Ce Dockerfile permet d'ajouter des dépendances spécifiques si nécessaire.

# Exemple : ajout de dépendances pour certains formats de quantification
# RUN pip install --no-cache-dir autoawq exllamav2

EXPOSE 8000
